version: '3.8'

services:
    bot:
        container_name: tg-manga-caption-bot
        build:
            context: .
            dockerfile: dockerfile
        stop_signal: SIGINT
        restart: on-failure
        env_file: .env